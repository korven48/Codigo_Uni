#include "Puzzle.h"

void mainPuzzle(tPuzzle& p) {
	/* 
		Function  that  solves  a  puzzle.  It  corresponds  to  a reduced version of the old mainGame   PM function.
	*/
}

bool load(tPuzzle& p, string type) {
	/*
		Opens  the  corresponding  file  and  calls  the  load  function  of  the  Matrix  module.  
		It  is  based  on  the  old  load function  of  the  GamePM module.  
		Now  you  do  not  have  to  ask  the  user  for  the  name  of  the  file  because  this  
		information is within the tPuzzle p.
	*/
	bool ok = true;

	return ok;
}

void show(const tPuzzle& p) {

}

bool play(tPuzzle& p) {
	ok = false;

	return ok;
}

void action(tPuzzle& p); {

}